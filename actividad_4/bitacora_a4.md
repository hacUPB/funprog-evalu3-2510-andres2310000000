Preguntas de control de comprensión  
  
1. ¿Para qué sirve un prototipo de función?    
Este informa al compilador como debe ser usada la funcion, tambien permite llamadas antes de que la funcion sea definida.  

2. ¿Cuál es la diferencia entre parámetros y argumentos?  
Los parametros son las variables que se declaran en el prototipo de la funcion, mientras que los argumentos son los valores que se pasan a la funcion cuando se llama.  

3. ¿Qué sucede si no incluyes el archivo de cabecera de una función de biblioteca estándar?  
Aparecen errores, y esto puede hacer que al momento de compilar ocurran cosas erroneas a lo deseado.  

4. ¿Por qué `main()` es especial en C?  O
Porque desde `main` es donde comienza la ejecución, se podria decir que es la entrada del programa.  

5. ¿Qué ocurre si una función no retorna ningún valor?    
Ocurre un error, si una funcion no retorna ningun valor debe usarse `void`.  
