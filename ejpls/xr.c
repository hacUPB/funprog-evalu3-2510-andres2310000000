#include <stdio.h>

int main(int argc, char const *argv[])
{
    float n1,n2, r;
    char operador;
    int x;
    do
    {
        printf("ingrese un numero, luego el operador, y luego el segundo numero");
        printf("operaciones disponibles:\n+\n-\n*\n/\n ");
        printf("")
    } while (operador !`?`);
    

    switch(x)
     {
        case 1:
            //x=1;
            printf("selecciono opc 1\n");
            break;
        case 2:
            x//=2
            printf("selecciono opc 2\n");
            break;
        
        default:
            printf("no selecciono nada\n");
            break;
    }
    return 0;
}

